import React, { Fragment } from "react";
import classes from "./Toolbar.module.css";

const toolbar = props => (
  <Fragment>
    <div className={classes.Toolbar}>TOOTLBAR</div>
  </Fragment>
);

export default toolbar;
